# Todos
- Review types of fields present in the logObserver class, I am doing a lot of explicit ! throughout the code and this is not elegant
- Strongly type the events generated by all classes
- Clean up the repo and name it something else than drum circle.
- Add an endpoint in the BE to create a circle
- Make the distrib log an abstract type so that it doesn't matter what is actually stored in the log
- All event names should be constants
- events that matter to the log observer.
- debug events (e.g log type change, term change etc)
- newPersistedEvent - when we are applying something to the state machine
- Iterator over peer pool so that I can ping each peer separatel

# The goal 
- A single html file that you can distribute, which 
  - Contains the game
  - Contains a debug panel
  - Contains a settings part for pointing to your signaling server of choice
- Typed events and strongly typed everything
- A separate library which contains the implementation of raft, over an abstract generic log, with configurable pruning and snapshotting

# Currently
- (active) Typing the event passing so we can have strongly typed events 
- (active) Fix the bug with disconnecting leader. Handle disconnects more gracefully. 

# SIGNALER
- Remove the circle id
- Refactor to implement eventTarget

# OVERALL
- Both the peerPool and the log observer are dependent upon the onConnect event from the signaler to assign their own peerId

# LOG OBSERVER
- Optimizations
  - Send batches of event instead of one event at a time
- Observability
  - Add console.log to all of the behaviour so we can keep track of what is going on
- Currently dispatching the observable state changes a little bit randomly
- Cluster membership changes
  - Handle peers joining and leaving
  - Handle own node leaving (as a leader candidate etc etc)
- Review
  - Peer counting. In some places am using the newer getOpenPeer method, in others I am not
- Heartbeats
  - Review logic for heartbeats. How does the leader know that they are getting a reply to their heartbeat? A thought -> if follower is up to date, what is true for their follower state? If a follower is not when we send a heartbeat, what does that mean for the heartbeat, should that behave like a retry?
  - AppendRPC response
  - If a follower receives two appendMessageRPC, then replies twice, what then? Will we ack twice and update the state of this follower twice?
  - Stale message detection
  - Optimizations

- A lot of problems would be solved if the first entry in my log was hardcoded

- Instead of JSON messages, send binary RPCs (protobuf etc)
